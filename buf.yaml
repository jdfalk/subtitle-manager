# file: buf.yaml
# version: 2.3.0
# guid: 8f3c2d5e-6a7b-4c8d-9e0f-1b2c3d4e5f6a

# Buf configuration for subtitle-manager
# Temporary configuration without gcommon dependency
# TODO: Re-add gcommon dependency when protobuf integration is complete

version: v2
modules:
  - path: proto
deps:
  - buf.build/googleapis/googleapis
  - buf.build/protocolbuffers/protobuf
  # TODO: Re-enable when gcommon protobuf is ready
  # - buf.build/jdfalk/gcommon
lint:
  use:
    - STANDARD
  except:
    - IMPORT_NO_PUBLIC
    - PACKAGE_DIRECTORY_MATCH
breaking:
  use:
    - FILE
